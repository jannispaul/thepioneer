import React from "react"
import styled from "styled-components"

const StyledSection = styled.section`
  background: black;
  width: 100%;
`

const HeroVideo = () => (
  <StyledSection>
    <h1>Live-Journalismus auf the Pioneer</h1>
  </StyledSection>
)

export default HeroVideo
